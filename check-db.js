require("dotenv").config(); const { PrismaClient } = require("@prisma/client"); const prisma = new PrismaClient(); async function main() { try { const records = await prisma.productoEnLinea.findMany(); console.log("Total records:", records.length); for (let i = 0; i < records.length; i++) { console.log(`Record ${i + 1}:`, { id: records[i].id, velocidadProduccion: records[i].velocidadProduccion }); } } catch (err) { console.error("Error:", err); } finally { await prisma.$disconnect(); } } main();
