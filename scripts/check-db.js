require('dotenv').config(); const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { try { const sample = await prisma.productoEnLinea.findFirst({ include: { producto: true, lineaProduccion: true } }); console.log('Sample ProductoEnLinea document:', JSON.stringify(sample, null, 2)); } catch (err) { console.error('Error:', err); } finally { await prisma.$disconnect(); } } main();
