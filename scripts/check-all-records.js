require('dotenv').config(); const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { try { const records = await prisma.productoEnLinea.findMany(); console.log('Total records:', records.length); records.forEach((record, index) => { console.log(, { id: record.id, velocidadProduccion: record.velocidadProduccion }); }); } catch (err) { console.error('Error:', err); } finally { await prisma.$disconnect(); } } main();
